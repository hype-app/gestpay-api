
## Encrypt Example: CashU

> CashU example: 

```xml
<Encrypt>
  <shopLogin>90000001</shopLogin>
  <uicCode>242</uicCode><!-- Euro -->
  <amount>0.10</amount>
  <shopTransactionId>MYSHOP_001</shopTransactionId>
  <paymentTypes>
    <paymentType>CASHU</paymentType>
  </paymentTypes>
</Encrypt>
```

CashU does not need mandatory parameters. 

You must use `CASHU` as `paymentType`.

[`OrderDetails`](#orderdetails) is not mandatory; However if you send it, `OrderDetails.BillingAddress.CountryCode` must be one of the following:  

`[AA, AE, BH, CY, DE, DZ, EG, ES, FR, GB, GH, IE, IL, IQ, IT, JO, KW, LB, NG, OM, PL, PS, PT, QA, SA, SD, SI, TN, TR] `

More informations about CashU are [in the doc]( <%= config[:doc_url] %>/oth/cashu.html). 

